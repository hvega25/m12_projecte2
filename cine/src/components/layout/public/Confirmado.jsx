/*Pagina que le muestra el usuario su compra ya realizada*/

import React from "react";
const Confirmado = () => {
    return(
        <h1> Este es la página de confirmado</h1>
    );
}

export default Confirmado;